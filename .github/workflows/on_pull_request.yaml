---
name: Pull Request

"on":
  pull_request:
    branches:
      - main
    types:
      - opened
      - reopened
      - synchronize

jobs:
  on-pull-request:
    uses: superbestfriends/github-workflow-repo/.github/workflows/on_pull_request_generic.yaml@v1
